# The image build, per push — moved from ci-post-receive.yml when this repo went domain-event-only,
# the switch qits-githost and qits-artifacts already made. `checkout:` makes it a move rather than a
# rewrite: the run builds the pushed branch at the pushed sha, but the pipeline is DISCOVERED at main,
# so a pushed branch can no longer alter the CI that gates it. suppressCi restates what the
# post-receive convention did with `-o qits.no-ci`. Separate from ci-event-userflows.yml on purpose:
# that run is non-gating, a red verify must not cost the image.
event: SCMPublishCommit
when:
  # repoName, not repoId: the git host's own coordinate can be an opaque UUID since the
  # identity cutover, so a name match on repoId is not dependable. The push event echoes the
  # public name the push was addressed with, which always is; a rename updates this line in
  # the same campaign.
  - repoName: { exact: qits-stt-service }
    suppressCi: { exact: "false" }
checkout:
  branch: branch
  sha: sha
steps:
  - image: qits/build-images/ci-base:latest
    docker: true
    timeout-seconds: 3600
    script: |
      # The daemon runs this with `bash -c` and no -e, so without this an early failure would be
      # swallowed by the next command's exit code, and cd is told an image exists that does not
      # (the exact shape of the qits-workspaces IMAGE_MISSING incident). -u makes an unset registry
      # variable stop the step too.
      set -eu
      registry_host="${QITS_REGISTRY%%:*}"

      # The two --secret flags are the fleet's shape for a credential inside a build: the launcher
      # injects the run's commissioned idp client as
      # $QITS_COMMISSIONED_CLIENT_ID/$QITS_COMMISSIONED_CLIENT_SECRET, and a Dockerfile that needs it
      # mounts it on one RUN — never a --build-arg, which is recorded in a pullable image's history.
      # This build feeds them: the Dockerfile's mvnw RUN mounts the pair as the `qits-maven-network`
      # server credential behind `.qits-maven-settings.xml` — the answer to the registry's 401 once
      # reads are gated, unused literals until then. A run that commissioned nothing still builds
      # green: a missing env source is NOT an error (measured on buildx v0.36.1, the mounted
      # variable is simply unset).
      ref="$QITS_REGISTRY/$QITS_IMAGE_REPOSITORY/qits-stt:$QITS_CI_SHA"
      docker build \
        --add-host "$registry_host:host-gateway" \
        --build-arg QITS_MAVEN_REPOSITORY_URL="http://$QITS_REGISTRY/artifacts/maven/maven" \
        --build-arg QITS_MAVEN_CENTRAL_URL="${QITS_MAVEN_CENTRAL_MIRROR_URL:-}" \
        --secret id=qits-client-id,env=QITS_COMMISSIONED_CLIENT_ID \
        --secret id=qits-client-secret,env=QITS_COMMISSIONED_CLIENT_SECRET \
        -t "$ref" -f docker/Dockerfile .
      docker push "$ref"
      # NO `docker rmi "$ref"`: on a CI host it frees nothing — the layers stay in the builder's
      # cache either way — while under the containerd image store it drops the last reference to
      # content a concurrent build of this same Dockerfile may still be resolving. Two builds of
      # this one Dockerfile do run concurrently, main's and environment/dev's, which is the race
      # that broke the release of 2026-08-11.
